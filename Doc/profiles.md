# Using the role name "AdministratorAccess"
CLI default client Region [us-east-1]:
CLI default output format [json]:
CLI profile name [AdministratorAccess-374604322722]: Admin

To use this profile, specify the profile name using --profile, as shown:

**aws s3 ls --profile Admin**
------------------------------------------
# Using the role name "ReadOnlyAccess"
CLI default client Region [us-east-1]:
CLI default output format [json]:
CLI profile name [ReadOnlyAccess-374604322722]: readonly

To use this profile, specify the profile name using --profile, as shown:

**aws s3 ls --profile readonly**
---------------------------------------------------
# Using the role name "NetworkAdministrator"
CLI default client Region [us-east-1]:
CLI default output format [json]:
CLI profile name [NetworkAdministrator-374604322722]: network

To use this profile, specify the profile name using --profile, as shown:

**aws s3 ls --profile network**